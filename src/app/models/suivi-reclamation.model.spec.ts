import { SuiviReclamation } from './suivi-reclamation.model';

describe('SuiviReclamation', () => {
  it('should create an instance', () => {
    expect(new SuiviReclamation()).toBeTruthy();
  });
});
