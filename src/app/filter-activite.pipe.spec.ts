import { FilterActivitePipe } from './filter-activite.pipe';

describe('FilterActivitePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActivitePipe();
    expect(pipe).toBeTruthy();
  });
});
