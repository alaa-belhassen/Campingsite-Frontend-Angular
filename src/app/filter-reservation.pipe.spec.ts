import { FilterReservationPipe } from './filter-reservation.pipe';

describe('FilterReservationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReservationPipe();
    expect(pipe).toBeTruthy();
  });
});
