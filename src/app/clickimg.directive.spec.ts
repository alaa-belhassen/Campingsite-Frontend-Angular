import { ClickimgDirective } from './clickimg.directive';

describe('ClickimgDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickimgDirective();
    expect(directive).toBeTruthy();
  });
});
